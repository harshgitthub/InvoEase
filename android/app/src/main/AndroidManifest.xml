<manifest xmlns:android="http://schemas.android.com/apk/res/android">


// Normal Permissions
<uses-permission android:name="android.permission.INTERNET" />
// Allows applications to open network sockets. Necessary for internet communication.

<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
// Allows applications to access information about networks. Useful for network-related operations.

<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
// Allows applications to access information about Wi-Fi networks. Useful for Wi-Fi related operations.

<uses-permission android:name="android.permission.BLUETOOTH" />
// Allows applications to connect to paired bluetooth devices. Required for Bluetooth functionality.

<uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
// Allows applications to discover and pair bluetooth devices. Required for managing Bluetooth connections.
// Dangerous Permissions (Need user consent at runtime)

<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
// Allows an application to read from external storage. Required for accessing files on external storage.

<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
// Allows an application to write to external storage. Required for saving files to external storage.

<uses-permission android:name="android.permission.CAMERA" />
// Allows applications to use the camera device. Necessary for camera-related operations.

<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
// Allows an application to access precise location using GPS, Wi-Fi, and mobile networks.

<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
// Allows an application to access approximate location using network-based sources.
<uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
// Allows an application to access approximate location using network-based sources.

<uses-permission android:name="android.permission.READ_CONTACTS" />
// Allows an application to read the user's contacts data. Required for accessing contacts.

<uses-permission android:name="android.permission.WRITE_CONTACTS" />
// Allows an application to write the user's contacts data. Required for modifying or adding contacts.

<uses-permission android:name="android.permission.SEND_SMS" />
// Allows an application to send SMS messages. Necessary for sending text messages.

<uses-permission android:name="android.permission.READ_SMS" />
// Allows an application to read SMS messages. Required for reading received SMS messages.

<uses-permission android:name="android.permission.RECEIVE_SMS" />
// Allows an application to receive SMS messages. Required for receiving and processing incoming SMS messages.

<uses-permission android:name="android.permission.CALL_PHONE" />
// Allows an application to initiate a phone call without user interaction.

<uses-permission android:name="android.permission.READ_PHONE_STATE" />
// Allows read-only access to phone state, including the current cellular network information and status of any ongoing calls.

    <application
        android:label="InvoEase"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:enableOnBackInvokedCallback="true"
            android:requestLegacyExternalStorage="true"
            >
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
